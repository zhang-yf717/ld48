using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Slime : Actor
{
    public override void Attack(GameObject target) {
        base.Attack(target);
    }

    public override void Move(Vector3 loc) {
        base.Move(loc);
    }
}
